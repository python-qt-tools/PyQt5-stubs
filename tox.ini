[tox]
envlist = py37

[testenv]
deps =
  mypy
  pytest
  pip>=20.0
  PyQt5==5.14.*
commands = 
  mypy -p PyQt5-stubs
  pytest {posargs}

[pytest]
addopts = --strict
testpaths = tests
xfail_strict = true
